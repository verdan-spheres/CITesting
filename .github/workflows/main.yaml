name: Min Htaw Lon
  on: 
    pull_request:
      branches:
      -dev

  jobs:
    build:
      name: Build Docker Images
      runs-on: ubuntu-latest
      steps: 
      -name: Testing step
      run: |
          echo "Hello Wrold"
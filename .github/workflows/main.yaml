name: Laravel CI

on:
  pull_request:
    branches:
      - dev

jobs:
  laravel-tests:
    runs-on: ubuntu-latest
      steps: 
        - name: Setup PHP with tools
            uses: shivammathur/setup-php@v2
            with:
              php-version: '8.3'
        - name: Testing step
          run: |
          echo "Hello Wrold"